/**
 * @fileoverview Firestore Security Rules
 *
 * Core Philosophy:
 * This ruleset provides basic protection against unauthorized access
 * by requiring users to be signed in for any data access.
 *
 * Data Structure:
 * The Firestore database has no defined schema based on the provided backend.json.
 *
 * Key Security Decisions:
 * - Requires authentication for all data access to prevent public access.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if the user is signed in
    function isSignedIn() {
      return request.auth != null;
    }

    // Default rule: Require authentication for all data access
    match /{document=**} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }
  }
}