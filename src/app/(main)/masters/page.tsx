import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function MastersPage() {
  return (
    <Card>
        <CardHeader>
            <CardTitle>Maestros</CardTitle>
            <CardDescription>Administra los perfiles de tus profesionales.</CardDescription>
        </CardHeader>
        <CardContent>
            <p>El contenido para la gestión de maestros estará aquí.</p>
        </CardContent>
    </Card>
  );
}
