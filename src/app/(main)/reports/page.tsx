import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function ReportsPage() {
  return (
    <Card>
        <CardHeader>
            <CardTitle>Reportes</CardTitle>
            <CardDescription>Genera reportes personalizados y exporta tus datos.</CardDescription>
        </CardHeader>
        <CardContent>
            <p>El generador de reportes estará aquí.</p>
        </CardContent>
    </Card>
  );
}
